version: '3.8'

services:
  kiro-go:
    image: kiro-go:${KIRO_GO_VERSION:-1.13}
    build: .
    ports:
      - "8088:8088"
    volumes:
      - ./data:/app/data
    environment:
      - CONFIG_PATH=/app/data/config.json
    restart: unless-stopped
